<% if @suggested_friends || @results %>

  <div id="users_index" >
    <h2 class="title-network">Suggestion de contacts:</h2>
    <div class="cards-homepage">
      <div class="row row-homepage">

        <% if @results %>
        <% array = @results %>
        <% else %>
          <% array = @suggested_friends %>
        <% end %>
        <% array.each do |user| %>
          <%= render "networks/searchable_users/user_card", user: user[0], work: user[1], number: user[2] %>

        <% end %>
        <div class="center">
          <%= link_to search_path(query: params[:query], more: true), class: "btn-plus"  do %>
            <%= "Voir plus" %>
          <% end unless params[:more] %>
        </div>

      </div>
    </div>
  </div>

<% else %>

  <p>Aucun résultat</p>

<% end %>
