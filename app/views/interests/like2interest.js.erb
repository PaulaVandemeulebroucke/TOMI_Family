function refreshCard(outerHTML) {
  const interest = document.getElementById(<%= @interest.id %>);
  interest.outerHTML = outerHTML;
}

refreshCard('<%= j render "interests/card", interest:@interest %>');
